{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run start:dev",
      "name": "start:dev",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    },
  ]
}